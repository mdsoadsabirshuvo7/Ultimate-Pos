@extends('cms::frontend.layouts.app')
@section('title', 'Sector 71 | Advanced SaaS for Modern Business')

@php
    $navbar_btn['text'] = 'Get Started';
    $navbar_btn['link'] = route('business.getRegister');
    if(isset($__site_details['btns']) && isset($__site_details['btns']['navbar']) && !empty($__site_details['btns']['navbar']['text'])) {
        $navbar_btn['text'] = $__site_details['btns']['navbar']['text'] ?? 'Get Started';
    }
    if(isset($__site_details['btns']) && isset($__site_details['btns']['navbar']) && !empty($__site_details['btns']['navbar']['link'])) {
        $navbar_btn['link'] = $__site_details['btns']['navbar']['link'] ?? route('business.getRegister');
    }

    $hero_btn['text'] = 'Get Started';
    $hero_btn['link'] = route('business.getRegister');
    if(isset($__site_details['btns']) && isset($__site_details['btns']['hero']) && !empty($__site_details['btns']['hero']['text'])) {
        $hero_btn['text'] = $__site_details['btns']['hero']['text'] ?? 'Get Started';
    }
    if(isset($__site_details['btns']) && isset($__site_details['btns']['hero']) && !empty($__site_details['btns']['hero']['link'])) {
        $hero_btn['link'] = $__site_details['btns']['hero']['link'] ?? route('business.getRegister');
    }

    $demo_link = Route::has('pricing') ? action([\Modules\Superadmin\Http\Controllers\PricingController::class, 'index']) : route('cms.contact.us');
    $social_logo = asset('modules/cms/img/sector71-social.jpg');
    $logo_img = asset('modules/cms/img/sector71-logo.jpg');
    $dashboard_preview = asset('modules/cms/img/home.png');
@endphp

@section('meta')
    <meta name="description" content="Empowering Retail, Pharmacy, and Dining owners with intuitive software solutions. Control your business, achieve your growth.">
    <meta property="og:title" content="Sector 71 | Advanced SaaS for Modern Business">
    <meta property="og:description" content="Empowering Retail, Pharmacy, and Dining owners with intuitive software solutions. Control your business, achieve your growth.">
    <meta property="og:type" content="website">
    <meta property="og:url" content="{{ url('/') }}">
    <meta property="og:image" content="{{ $social_logo }}">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:title" content="Sector 71 | Advanced SaaS for Modern Business">
    <meta property="twitter:description" content="Empowering Retail, Pharmacy, and Dining owners with intuitive software solutions. Control your business, achieve your growth.">
    <meta property="twitter:image" content="{{ $social_logo }}">
@endsection

@section('css')
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Hind+Siliguri:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --s71-navy: #0A192F;
            --s71-blue: #4AA7FF;
            --s71-accent: #21D07A;
            --s71-ink: #020C1B;
            --s71-surface: #112240;
            --s71-muted: #9AB3D7;
            --s71-grid: rgba(136, 146, 176, 0.03);
            --s71-glow: rgba(33, 208, 122, 0.32);
        }

        body {
            font-family: 'Inter', sans-serif;
            background: radial-gradient(1200px 520px at 8% -8%, rgba(74, 167, 255, 0.13), transparent 65%), linear-gradient(180deg, var(--s71-ink) 0%, var(--s71-navy) 100%);
            color: #E6F1FF;
        }

        .lang-bn,
        html[data-lang='bn'] .lang-en {
            display: none;
        }

        html[data-lang='bn'] .lang-bn {
            display: block;
            font-family: 'Hind Siliguri', sans-serif;
        }

        .s71-page {
            overflow-x: hidden;
            background-image: linear-gradient(var(--s71-grid) 1px, transparent 1px), linear-gradient(90deg, var(--s71-grid) 1px, transparent 1px);
            background-size: 48px 48px;
        }

        .s71-section {
            padding: 78px 0;
            position: relative;
        }

        .s71-lang-toggle {
            position: fixed;
            right: 18px;
            top: 98px;
            z-index: 999;
            border-radius: 999px;
            border: 1px solid #2A4569;
            background: rgba(10, 25, 47, 0.94);
            padding: 4px;
            display: flex;
            gap: 4px;
            box-shadow: 0 10px 24px rgba(2, 12, 27, 0.45);
        }

        .s71-lang-btn {
            border: 0;
            background: transparent;
            color: #CCD8F2;
            font-size: .82rem;
            font-weight: 700;
            padding: 6px 12px;
            border-radius: 999px;
        }

        .s71-lang-btn.active {
            background: var(--s71-accent);
            color: #08281D;
        }

        .s71-hero {
            min-height: 92vh;
            padding-top: 20px;
            display: flex;
            align-items: center;
        }

        .s71-kicker {
            color: var(--s71-blue);
            letter-spacing: .1em;
            font-weight: 700;
            text-transform: uppercase;
            font-size: .78rem;
        }

        .s71-title {
            font-size: clamp(2.1rem, 5vw, 3.7rem);
            line-height: 1.08;
            font-weight: 800;
            margin: 14px 0 16px;
        }

        .s71-sub {
            color: #C6D5F2;
            font-size: clamp(1rem, 1.7vw, 1.18rem);
            max-width: 720px;
        }

        .s71-cta-row {
            display: flex;
            flex-wrap: wrap;
            gap: 14px;
            margin-top: 25px;
        }

        .s71-btn-primary {
            border: 1px solid var(--s71-accent);
            background: var(--s71-accent);
            color: #062117;
            font-weight: 700;
            border-radius: 12px;
            padding: 11px 24px;
            box-shadow: 0 12px 28px var(--s71-glow);
        }

        .s71-btn-primary:hover {
            color: #062117;
            transform: translateY(-2px);
        }

        .s71-btn-secondary {
            border: 1px solid #2E4E74;
            background: rgba(17, 34, 64, .58);
            color: #E6F1FF;
            font-weight: 600;
            border-radius: 12px;
            padding: 11px 24px;
        }

        .s71-btn-secondary:hover {
            color: var(--s71-blue);
            border-color: var(--s71-blue);
        }

        .s71-badges {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            margin-top: 16px;
        }

        .s71-badge {
            border: 1px solid #2A4569;
            background: rgba(17, 34, 64, 0.68);
            color: #C9D9F5;
            border-radius: 999px;
            padding: 7px 12px;
            font-size: .85rem;
        }

        .s71-human-visual {
            position: relative;
            border-radius: 18px;
            overflow: hidden;
            border: 1px solid #29476B;
            min-height: 470px;
            box-shadow: 0 16px 36px rgba(2, 12, 27, 0.62);
        }

        .s71-human-visual img.bg {
            width: 100%;
            height: 100%;
            object-fit: cover;
            filter: brightness(.66) saturate(1.05);
        }

        .s71-overlay-ui {
            position: absolute;
            width: 70%;
            right: 14px;
            bottom: 14px;
            background: rgba(10, 25, 47, 0.9);
            border: 1px solid #2E567E;
            border-radius: 14px;
            overflow: hidden;
            backdrop-filter: blur(8px);
        }

        .s71-logo-badge {
            position: absolute;
            width: 86px;
            height: 86px;
            right: 18px;
            top: 18px;
            border-radius: 50%;
            border: 1px solid #375d87;
            background: rgba(2, 12, 27, 0.85);
            box-shadow: 0 10px 24px rgba(2, 12, 27, 0.6);
            object-fit: cover;
            padding: 8px;
        }

        .s71-overlay-ui img {
            width: 100%;
            display: block;
        }

        .s71-overlay-chip {
            position: absolute;
            left: 14px;
            top: 14px;
            background: rgba(2, 12, 27, .82);
            border: 1px solid #355980;
            border-radius: 10px;
            padding: 7px 10px;
            font-size: .82rem;
            color: #D5E2FA;
        }

        .s71-card {
            background: linear-gradient(180deg, rgba(17, 34, 64, .84) 0%, rgba(10, 25, 47, .92) 100%);
            border: 1px solid #223F62;
            border-radius: 16px;
            padding: 22px;
            height: 100%;
        }

        .s71-card h3 {
            margin: 0 0 8px;
            font-size: 1.18rem;
            color: #E7F0FF;
        }

        .s71-card p {
            margin: 0;
            color: #B7C8E7;
        }

        .s71-icon {
            width: 42px;
            height: 42px;
            border-radius: 10px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 12px;
            background: rgba(74, 167, 255, .15);
            color: var(--s71-blue);
            font-size: 1.1rem;
        }

        .s71-section-title {
            font-size: clamp(1.65rem, 3vw, 2.35rem);
            font-weight: 800;
            margin-bottom: 10px;
        }

        .s71-section-sub {
            color: #A7BADB;
            max-width: 780px;
        }

        .s71-table-wrap {
            border: 1px solid #2A456A;
            border-radius: 16px;
            overflow: hidden;
            background: rgba(17, 34, 64, .74);
        }

        .s71-table {
            width: 100%;
            border-collapse: collapse;
        }

        .s71-table th,
        .s71-table td {
            border-bottom: 1px solid rgba(138, 161, 194, 0.2);
            padding: 14px;
            text-align: left;
            color: #D6E3FB;
            font-size: .95rem;
        }

        .s71-table th {
            background: rgba(10, 25, 47, 0.85);
            color: #9EC2F2;
            font-weight: 700;
        }

        .s71-table tr:last-child td {
            border-bottom: 0;
        }

        .s71-price-highlight {
            color: var(--s71-accent);
            font-weight: 700;
        }

        .s71-local {
            border: 1px solid #2B4A6F;
            border-radius: 16px;
            padding: 22px;
            background: rgba(17, 34, 64, .68);
        }

        .reveal {
            opacity: 0;
            transform: translateY(14px);
            transition: opacity .55s ease, transform .55s ease;
        }

        .reveal.is-visible {
            opacity: 1;
            transform: translateY(0);
        }

        @media (max-width: 991px) {
            .s71-lang-toggle {
                top: auto;
                bottom: 16px;
                right: 16px;
            }

            .s71-hero {
                min-height: auto;
                padding-top: 10px;
            }

            .s71-human-visual {
                min-height: 340px;
                margin-top: 16px;
            }

            .s71-overlay-ui {
                width: 78%;
            }

            .s71-table th,
            .s71-table td {
                padding: 10px;
                font-size: .88rem;
            }
        }
    </style>
@endsection

@section('content')
<div class="s71-page">
    <div class="s71-lang-toggle" role="group" aria-label="Language toggle">
        <button class="s71-lang-btn active" data-set-lang="en" type="button">EN</button>
        <button class="s71-lang-btn" data-set-lang="bn" type="button">বাং</button>
    </div>

    <section class="s71-hero s71-section">
        <div class="container">
            @includeIf('cms::frontend.layouts.navbar')
            <div class="row align-items-center g-4 mt-3">
                <div class="col-lg-6 reveal">
                    <div class="s71-kicker lang-en">Access granted. Control achieved.</div>
                    <div class="s71-kicker lang-bn">সম্পূর্ণ নিয়ন্ত্রণ এখন আপনার হাতে</div>

                    <h1 class="s71-title lang-en">Master Your Operations. Maximize Your Growth.</h1>
                    <h1 class="s71-title lang-bn">ব্যবসা পরিচালনা করুন সহজে, বৃদ্ধি করুন দ্রুত।</h1>

                    <p class="s71-sub lang-en">Advanced SaaS solutions for Retail, Pharmacy, and Dining. Access granted. Control achieved.</p>
                    <p class="s71-sub lang-bn">রিটেইল, ফার্মেসি এবং রেস্টুরেন্টের জন্য উন্নত SaaS সমাধান। সম্পূর্ণ নিয়ন্ত্রণ এখন আপনার হাতে।</p>

                    <div class="s71-cta-row">
                        <a href="{{ $hero_btn['link'] }}" class="s71-btn-primary">Get Started</a>
                        <a href="{{ $demo_link }}" class="s71-btn-secondary">View Demo</a>
                    </div>

                    <div class="s71-badges">
                        <span class="s71-badge">🇧🇩 Made in Bangladesh</span>
                        <span class="s71-badge">Local Support | Rajshahi</span>
                    </div>
                </div>

                <div class="col-lg-6 reveal">
                    <div class="s71-human-visual">
                        <img class="bg" src="https://images.unsplash.com/photo-1556740749-887f6717d7e4?auto=format&fit=crop&w=1200&q=80" alt="Modern retail counter using POS tablet" loading="lazy">
                        <div class="s71-overlay-chip">Live Retail Counter</div>
                        <img class="s71-logo-badge" src="{{ $logo_img }}" alt="Sector 71 logo" loading="lazy">
                        <div class="s71-overlay-ui">
                            <img src="{{ $dashboard_preview }}" alt="Sector 71 dashboard interface overlay" loading="lazy">
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="s71-section" id="power-trio">
        <div class="container">
            <div class="reveal mb-4">
                <h2 class="s71-section-title lang-en">The Power Trio</h2>
                <h2 class="s71-section-title lang-bn">টার্গেট ব্যবসা সমাধান</h2>
                <p class="s71-section-sub lang-en">Built to match daily workflows in Retail, Pharmacy, and Dining.</p>
                <p class="s71-section-sub lang-bn">রিটেইল, ফার্মেসি এবং ডাইনিং ব্যবসার দৈনন্দিন কাজের জন্য কাস্টমাইজড প্ল্যাটফর্ম।</p>
            </div>
            <div class="row g-4">
                <div class="col-lg-4 reveal">
                    <div class="s71-card">
                        <div class="s71-icon"><i class="fas fa-store"></i></div>
                        <h3>Retail and Clothing</h3>
                        <p>Multi-variation inventory, barcode printing, and SMS alerts.</p>
                    </div>
                </div>
                <div class="col-lg-4 reveal">
                    <div class="s71-card">
                        <div class="s71-icon"><i class="fas fa-clinic-medical"></i></div>
                        <h3>Pharmacy Management</h3>
                        <p>Batch tracking, expiry date alerts, and complete drug database flow.</p>
                    </div>
                </div>
                <div class="col-lg-4 reveal">
                    <div class="s71-card">
                        <div class="s71-icon"><i class="fas fa-concierge-bell"></i></div>
                        <h3>Dining and Restaurants</h3>
                        <p>QR menus, KOT printing, and table management for fast service.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="s71-section" id="localized-pricing">
        <div class="container">
            <div class="reveal mb-4">
                <h2 class="s71-section-title">আমাদের প্ল্যানসমূহ (Pricing Plans)</h2>
                <p class="s71-section-sub">লোকাল ব্যবসার জন্য সহজ ও স্বচ্ছ টাকার প্যাকেজ।</p>
            </div>
            <div class="s71-table-wrap reveal">
                <table class="s71-table">
                    <thead>
                        <tr>
                            <th>ক্যাটাগরি (Plan)</th>
                            <th>যাদের জন্য (Best For)</th>
                            <th>মাসিক ফি (Monthly)</th>
                            <th>বাৎসরিক ফি (Yearly)</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>স্টার্টার (Starter)</td>
                            <td>জেনারেল স্টোর ও ছোট দোকান</td>
                            <td>৳৯৯৯</td>
                            <td class="s71-price-highlight">৳১০,০০০ (৳২,০০০ সাশ্রয়!)</td>
                        </tr>
                        <tr>
                            <td>বিজনেস (Business)</td>
                            <td>ক্লোথিং স্টোর ও সুপারশপ</td>
                            <td>৳১,৪৯৯</td>
                            <td>৳১৫,০০০</td>
                        </tr>
                        <tr>
                            <td>প্রিমিয়াম (Premium)</td>
                            <td>রেস্টুরেন্ট ও ক্যাফে</td>
                            <td>৳২,৪৯৯</td>
                            <td>৳২৫,০০০</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </section>

    <section class="s71-section" id="why-sector71">
        <div class="container">
            <div class="reveal mb-4">
                <h2 class="s71-section-title">Why Sector 71?</h2>
                <p class="s71-section-sub">Owner-first benefits that directly improve daily business operations.</p>
            </div>
            <div class="row g-4">
                <div class="col-md-4 reveal">
                    <div class="s71-card">
                        <div class="s71-icon"><i class="fas fa-wallet"></i></div>
                        <h3>বিকাশ/নগদ পেমেন্ট সাপোর্ট</h3>
                        <p>লোকাল পেমেন্ট মেথডের সাথে দ্রুত ইন্টিগ্রেশন।</p>
                    </div>
                </div>
                <div class="col-md-4 reveal">
                    <div class="s71-card">
                        <div class="s71-icon"><i class="fas fa-wifi"></i></div>
                        <h3>অফলাইন সাপোর্ট</h3>
                        <p>ইন্টারনেট না থাকলেও কাজ থামবে না।</p>
                    </div>
                </div>
                <div class="col-md-4 reveal">
                    <div class="s71-card">
                        <div class="s71-icon"><i class="fas fa-headset"></i></div>
                        <h3>২৪/৭ কাস্টমার সাপোর্ট</h3>
                        <p>রাজশাহী ভিত্তিক লোকাল সাপোর্ট টিম আপনার সেবায় সবসময়।</p>
                    </div>
                </div>
            </div>

            <div class="s71-local reveal mt-4">
                <p class="mb-2">Sector 71 Softwares | +8801336313953 (WhatsApp)</p>
                <p class="mb-2">sector71.owner@gmail.com</p>
                <p class="mb-0">https://sector71.app | https://sector71.codes</p>
            </div>
        </div>
    </section>
</div>
@endsection

@section('javascript')
<script>
    (function () {
        var html = document.documentElement;
        var storedLang = localStorage.getItem('s71_lang') || 'en';
        html.setAttribute('data-lang', storedLang);

        function refreshLangButtons() {
            document.querySelectorAll('[data-set-lang]').forEach(function (btn) {
                btn.classList.toggle('active', btn.getAttribute('data-set-lang') === html.getAttribute('data-lang'));
            });
        }

        document.querySelectorAll('[data-set-lang]').forEach(function (btn) {
            btn.addEventListener('click', function () {
                var lang = btn.getAttribute('data-set-lang');
                html.setAttribute('data-lang', lang);
                localStorage.setItem('s71_lang', lang);
                refreshLangButtons();
            });
        });
        refreshLangButtons();

        var revealItems = document.querySelectorAll('.reveal');
        if (!('IntersectionObserver' in window)) {
            revealItems.forEach(function (el) { el.classList.add('is-visible'); });
            return;
        }

        var io = new IntersectionObserver(function (entries) {
            entries.forEach(function (entry) {
                if (entry.isIntersecting) {
                    entry.target.classList.add('is-visible');
                    io.unobserve(entry.target);
                }
            });
        }, { threshold: 0.15 });

        revealItems.forEach(function (el) { io.observe(el); });
    })();
</script>
@endsection
